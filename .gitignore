/CLAUDE.md
/.claude/
__pycache__/
.env
# Generated per student — holds a Snowflake account plus a private key.
/profiles.yml
# Generated at container startup (the rest of /static/ is committed and shipped
# in the image — that's where Streamlit's static file serving reads from)
/static/container-info.json
.DS_Store
# Written when the app runs outside a container (dbt.log, query_log.sql).
/logs/
